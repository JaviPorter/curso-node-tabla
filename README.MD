# Notas:
Este es mi primer programa en node
```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Coge la base para crear la tabla de multiplicar
                                                            [número] [requerido]
  -l, --listar   Muestra por pantalla la tabla de multiplicar
                                                     [booleano] [defecto: false]
  -h, --hasta    Hasta que valor hemos de sacar la tabla           [defecto: 10]
```